IA d'un  fournisseur !!!

liste = []
for i in range(len(cellsCapturees // 2)):
    A = cellsCaptures[i]
    
    pour chaque fournisseur f dans la liste des fournisseurs de laPartie à faire jouer :
        liste.append([f,distanceEntreFetA])

    trier liste selon les distances du minimum au maximum (extraites du Noeud)
    
    j = 0
    total = différence des forces qui vont capturer notre noeud A (A.atk+A.def - lemouvementencours.nbUnites)
    tant que j < len(liste) or total > 0 :
        si f.atk - total >= 0 :
            envoyer de f vers A total + (f.atk-total)/2
            total -= total
        sinon :
            envoyer de f vers A f.atk unites
            enlever f des noeuds de la liste
            total -= f.atk
        j++


refaire pareil avec la liste des cellules en danger

puis pour chaque fournisseur encore restant :
    envoyer aux rushers proches si en début de partie
    envoyer aux attaquants proches sinon